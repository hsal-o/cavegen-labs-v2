class GenericInputController:
    def __init__(self, view, service):
        self.view = view
        self.service = service

    def update_service(self):
        # size = self.view.size_entry.get()
        # self.service.set_grid_size(size)
        return

    def set_view_from_service(self):
        # size = self.service.get_grid_size()
        # self.view.set_grid_size(size)
        return
